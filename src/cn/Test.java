package cn;

import reactor.core.publisher.Flux;

import java.util.stream.Stream;

public class Test {
    public  static void  main(String[] args){
      //  Flux.just(1,2,3,4,5,6).filter(i->i%2==0).map(integer -> integer+2).
       // int age= Stream.of(new Person[])
    }
}
